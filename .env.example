# Joan Dashboard — Environment Configuration
# Copy this file to .env and fill in your values.
# The dashboard reads these at startup.

# === Visionect Software Suite (VSS) ===
# IP/hostname of the machine running VSS (e.g. Raspberry Pi)
VSS_HOST=192.168.1.100
# VSS HTTP API port (default 8081)
VSS_PORT=8081
# Joan device UUIDs — comma-separated (find in VSS web UI → Devices)
# All allowed devices are auto-discovered if omitted.
# Single UUID also works: DEVICE_UUID=your-uuid-here
DEVICE_UUIDS=uuid1,uuid2
# VSS admin credentials (defaults shown)
VSS_USER=admin
VSS_PASS=visionect1

# === Weather (Open-Meteo, free, no API key needed) ===
# Your location coordinates (use https://www.latlong.net/)
WEATHER_LAT=51.845
WEATHER_LON=-0.943
WEATHER_LOCATION=Waddesdon

# === UK Trains (Rail Data Marketplace — free key at raildata.org.uk) ===
# Subscribe to "Live Departure Board" at https://raildata.org.uk
TRAINS_API_KEY=your-rdm-api-key-here
# Station CRS codes: https://www.nationalrail.co.uk/stations_destinations/48702.aspx
TRAINS_STATION=AVP
TRAINS_DESTINATION=MYB

# === Bin Collection (Buckinghamshire Council via iTouchVision API) ===
# Find your UPRN at https://uprn.uk or https://www.findmyaddress.co.uk
BIN_UPRN=766351323
